// Package agents contains rule-based implementations of all shop agents.
// No API key is required. When an Anthropic API key becomes available,
// replace the implementations in search.go, recommend.go, and devis_chat.go
// with the LLM-backed versions.
package agents
